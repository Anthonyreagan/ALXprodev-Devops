#!/bin/bash

# Extract and format Pikachu's data from data.json
jq -r '
  "\(.name) is of type \(.types[0].type.name), weighs \(.weight/10)kg, and is \(.height/10)m tall."
' data.json | 
sed 's/^./\U&/'  # Capitalize first letter